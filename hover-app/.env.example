# Backend API base URL — used by the renderer (api.ts) and main process
# Change this to your Render URL before deploying (no trailing slash)
# Local dev:
VITE_API_BASE=http://localhost:8000/api/v1
# Production (uncomment and fill in):
# VITE_API_BASE=https://your-app.onrender.com/api/v1
